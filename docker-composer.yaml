version: '3.8'

services:
  progetto_its_finals:
    build: .
    container_name: progetto_its_finals

    ports:
      - "8082:8082"

    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: Bari2025
      DB_NAME: Progetto_ITS_Finals
      PORT: 8082

    restart: unless-stopped